# Example .env for Back_mensajeria
# Copy this file to .env and update values for your environment

# App
PORT=9000

# MySQL (used by parts of the app)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=changeme
MYSQL_DATABASE=mensajeria_disputas

# MongoDB (some modules use Mongo)
# Use full connection string: mongodb://user:pass@host:port/dbname
MONGODB_URI=mongodb://localhost:27017/mensajeria_disputas

# JWT
JWT_SECRET=change_this_secret_to_a_secure_value
JWT_EXPIRES_IN=1d

# CORS - allowed frontend origins (comma-separated)
FRONTEND_URLS=http://localhost:3000

# Optional: email / SMTP for notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user
# SMTP_PASS=pass

# Optional: third-party keys
# SOME_API_KEY=

# Notes:
# - Do NOT commit your .env to the repository. Keep secrets out of git.
# - Use a strong random string for JWT_SECRET in production.
